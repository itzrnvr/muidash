import React from 'react'   
import InternalDataGrid from './InternalDataGrid'

const SmartDataGrid = ({
  title, subTitle, 
  columns, rows, 
}) => {
  return (
    <InternalDataGrid/>
  )
}

export default SmartDataGrid